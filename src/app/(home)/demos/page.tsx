import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Demos",
  description: "Demos",
}

export default function DemosPage() {
  return (
    <main>
      <h1>Demos</h1>
    </main>
  )
}